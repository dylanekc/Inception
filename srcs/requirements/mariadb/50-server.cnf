[mysqld]
# Dossier où sont stockées les données physiques
datadir = /var/lib/mysql
socket  = /run/mysqld/mysqld.sock

# Autorise les connexions extérieures (essentiel pour WordPress)
bind-address = 0.0.0.0
port = 3306
user = mysql
